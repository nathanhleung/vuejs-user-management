{
  "name": "vuejs-user-management",
  "version": "1.0.0",
  "description": "Demo of user management app written in Vue.js",
  "main": "index.js",
  "scripts": {
    "build": "npm run jade && npm run stylus && npm run babel",
    "jade": "jade index.jade --out build",
    "stylus": "stylus styles.styl --out build",
    "babel": "babel app.js --out-dir build"
  },
  "author": "Nathan Leung",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13",
    "jade": "^1.11.0",
    "stylus": "^0.53.0"
  }
}
